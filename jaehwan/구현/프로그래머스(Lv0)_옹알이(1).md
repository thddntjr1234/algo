```
class Solution {
    private final static String[] PRONUNCIATIONS = {"aya", "ye", "woo", "ma"};
    public int solution(String[] babbling) {
        int answer = 0;
        StringBuilder sb = new StringBuilder();
        
        for(int i=0; i<babbling.length; i++){
            sb.append(babbling[i]);
            // 주어진 단어에서 발음 가능한 단어 제거
            for(int j=0; j<PRONUNCIATIONS.length; j++){
                int start = sb.indexOf(PRONUNCIATIONS[j]);
                if(start != -1){
                    int end = PRONUNCIATIONS[j].length();
                    sb.replace(start, start+end," ");
                }
            }
            // 공백 제거
            while(sb.indexOf(" ") != -1){
                int index = sb.indexOf(" ");
                sb.delete(index, index+1);
            }
            
            // 발음할수 있는 단어인지 검증
            if(sb.length() == 0){
                answer++;
            }
            
            sb.setLength(0);
        }
        
        return answer;
    }
}
```
### 문제 (https://school.programmers.co.kr/learn/courses/30/lessons/120956)

### 요구사항
- 발음 가능한 네 가지 단어 ("aya", "ye", "woo", "ma")
- babbling : 소문자로만 이루어진 문자열 배열
- 발음 가능한 단어를 최대 한번만 사용하여 babbling에서 발음 가능한 단어의 개수 반환

### 후기
- String 객체 불변성을 고려하여 StringBuilder를 사용하였으나, 현재 제한사항에선 성능상의 이점이 유의미하지 않으니 그냥 String써서 간결하게 구현해도 될듯.
- replace()가 아닌 delete()를 사용할 시 우연히 단어가 조합되는 케이스가 있으므로 주의하기.
